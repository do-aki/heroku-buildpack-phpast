#!/bin/sh

BUILD_DIR=$1
GIT_URL="https://github.com/do-aki/phpast"


cd $BUILD_DIR
echo $BUILD_DIR
git clone ${GIT_URL} phpast

cd phpast
phpize
./configure
make

cp modules/phpast.so `php-config --extension-dir`
echo "extension=phpast.so" > /app/.heroku/php/etc/php/conf.d/phpast.conf

